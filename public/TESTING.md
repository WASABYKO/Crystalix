# Тест-кейсы для проверки исправлений

## 1. Тест: Предотвращение дублирующей инициализации HashStorage

**Описание:** Проверка, что HashStorage инициализируется только один раз

**Шаги:**
1. Откройте консоль разработчика (F12)
2. Перейдите на любую страницу приложения
3. Проверьте логи на наличие сообщений:
   - `🚀 Инициализация HashStorage v3.1`
   - `⚠️ HashStorage уже инициализирован` (при повторном вызове)

**Ожидаемый результат:**
- Сообщение об инициализации появляется только один раз
- Сообщение `HashStorage already initialized` не появляется при обычной загрузке

**Критерии успеха:**
- [ ] Сообщение `HashStorage уже инициализирован` НЕ появляется при первой загрузке
- [ ] Страница загружается без ошибок
- [ ] Лоадер скрывается корректно

---

## 2. Тест: Обработка ошибки ERR_CONNECTION_REFUSED

**Описание:** Проверка fallback-механизма при недоступности сервера

**Шаги:**
1. Остановите сервер (`Ctrl+C` в терминале)
2. Обновите страницу приложения
3. Попробуйте войти с любыми данными

**Ожидаемый результат:**
- Появляется уведомление о проблемах с соединением
- Приложение переходит в offline-режим
- Страница НЕ уходит в бесконечный релоад

**Критерии успеха:**
- [ ] Появляется предупреждение о недоступности сервера
- [ ] Кнопка входа остается активной после ошибки
- [ ] Нет бесконечного цикла перезагрузки страницы
- [ ] В консоли есть сообщение от ConnectionManager

---

## 3. Тест: Блокировка после множественных неудачных попыток входа

**Описание:** Проверка ограничения попыток входа

**Шаги:**
1. Перейдите на страницу `/auth.html`
2. Введите НЕВЕРНЫЙ пароль 5+ раз подряд
3. Наблюдайте за поведением системы

**Ожидаемый результат:**
- После 5 неудачных попыток появляется сообщение о блокировке
- Временная блокировка на 1 минуту
- Возможность повторить попытку после разблокировки

**Критерии успеха:**
- [ ] После 5 неудачных попыток появляется сообщение
- [ ] Кнопка входа блокируется
- [ ] Таймер обратного отсчета виден
- [ ] Разблокировка происходит автоматически через 1 минуту

---

## 4. Тест: Повторные попытки с экспоненциальной задержкой

**Описание:** Проверка работы RetryManager

**Шаги:**
1. Временно остановите сервер
2. Попробуйте войти (должна быть 1-2 секунды между попытками)
3. Включите сервер

**Ожидаемый результат:**
- Система делает 2-3 попытки перед показом ошибки
- Задержка между попытками увеличивается (1с, 2с, 4с...)
- При успешном соединении - автоматический выход из fallback-режима

**Критерии успеха:**
- [ ] В консоли видны сообщения о повторных попытках
- [ ] Задержка между попытками растет экспоненциально
- [ ] При восстановлении сервера приложение выходит из offline-режима

---

## 5. Тест: WebSocket переподключение

**Описание:** Проверка работы WebSocketManager

**Шаги:**
1. Войдите в систему (авторизуйтесь)
2. Откройте консоль разработчика
3. Проверьте сообщения `[WS]`

**Ожидаемый результат:**
- WebSocket подключается после успешной авторизации
- При разрыве соединения - автоматическое переподключение
- Токен отправляется при переподключении

**Критерии успеха:**
- [ ] Сообщение `[WS] Соединение установлено` появляется
- [ ] При закрытии WS - сообщение `[WS] Переподключение через X мс`
- [ ] Автоматическое восстановление соединения

---

## 6. Тест: Глобальный обработчик ошибок

**Описание:** Проверка GlobalErrorHandler

**Шаги:**
1. Откройте консоль разработчика
2. Введите в консоли: `throw new Error('Test error')`
3. Проверьте логи

**Ожидаемый результат:**
- Ошибка перехватывается глобальным обработчиком
- В консоли есть сообщение `[GlobalErrorHandler]`
- Приложение продолжает работать

**Критерии успеха:**
- [ ] Ошибка не ломает приложение
- [ ] Сообщение в консоли от GlobalErrorHandler
- [ ] Страница остается функциональной

---

## 7. Тест: Навигация без бесконечного релоада

**Описание:** Проверка предотвращения бесконечного релоада

**Шаги:**
1. Откройте Network tab в DevTools
2. Перезагрузите страницу несколько раз быстро
3. Наблюдайте за запросами

**Ожидаемый результат:**
- Страница загружается стабильно
- Нет бесконечных циклов перенаправлений
- LoaderComponent скрывается корректно

**Критерии успеха:**
- [ ] Страница загружается с 1-2 попыток
- [ ] Нет сообщений о цикле редиректов
- [ ] LoaderComponent.hide() вызывается корректно

---

## Автоматические тесты (если настроены)

```javascript
// Пример unit-теста для ConnectionManager
test('ConnectionManager.identifyError классифицирует типы ошибок', () => {
    const networkError = new Error('Failed to fetch');
    const authError = new Error('Unauthorized');
    authError.status = 401;
    
    expect(ConnectionManager.identifyError(networkError)).toBe('NETWORK');
    expect(ConnectionManager.identifyError(authError)).toBe('AUTH');
});

// Пример unit-теста для SafeInitializer
test('SafeInitializer предотвращает дублирующую инициализацию', async () => {
    let initCount = 0;
    const mockInit = () => {
        initCount++;
        return Promise.resolve();
    };
    
    SafeInitializer.register('TestComponent', { initFn: mockInit });
    await SafeInitializer.initialize('TestComponent');
    await SafeInitializer.initialize('TestComponent');
    
    expect(initCount).toBe(1);
});
```

---

## Чек-лист перед деплоем

- [ ] Проверить все 7 тест-кейсов
- [ ] Убедиться, что логи чистые (нет дублирующих сообщений)
- [ ] Проверить работу без сервера (offline mode)
- [ ] Проверить поведение при блокировке входа
- [ ] Убедиться в отсутствии memory leaks
- [ ] Проверить совместимость с HashStorage v3.1
