# TechTariff Messenger

Современный мессенджер с реал-тайм чатами, авторизацией и загрузкой файлов.

## 🛠 Технологии

- **Frontend**: HTML5, CSS3, Vanilla JavaScript (ES6+)
- **Backend**: Node.js, Express.js
- **Real-time**: WebSocket (ws)
- **Database**: SQLite
- **Authentication**: JWT (JSON Web Tokens)
- **File Upload**: Multer

## 📁 Структура проекта

```
TechTariff/
├── public/                 # Статические файлы (фронтенд)
│   ├── index.html         # Главная страница
│   ├── auth.html          # Страница авторизации
│   ├── dashboard.html     # Личный кабинет
│   ├── messages.html      # Чат
│   ├── admin.html         # Админ-панель
│   ├── css/               # Стили
│   ├── js/                # Клиентский JavaScript
│   │   ├── app.js        # Основной файл приложения
│   │   ├── auth.js       # Модуль авторизации
│   │   ├── managers/     # Менеджеры приложения
│   │   └── components/   # UI компоненты
│   ├── sounds/            # Звуковые уведомления
│   └── uploads/           # Загруженные файлы
├── src/
│   └── server/            # Серверная часть
│       ├── index.js       # Точка входа
│       ├── app.js         # Express приложение
│       ├── ws.js          # WebSocket сервер
│       ├── database/      # Работа с БД
│       ├── routes/        # API маршруты
│       │   ├── auth.js    # Авторизация
│       │   ├── users.js   # Пользователи
│       │   └── files.js   # Файлы
│       ├── models/        # Модели данных
│       └── uploads/       # Загруженные файлы (сервер)
├── data/                  # JSON данные (демо)
├── logs/                  # Логи приложения
├── .env                   # Переменные окружения
├── package.json           # Зависимости
└── .gitignore             # Исключения Git
```

## 🚀 Установка и запуск

### Предварительные требования

- Node.js >= 18.0.0
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Запуск в продакшене

```bash
npm start
```

Сервер запустится на http://localhost:3000

## 🔧 Конфигурация

Скопируйте `.env.example` в `.env` и заполните переменные:

```bash
cp .env.example .env
```

### Основные переменные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | 3000 |
| `NODE_ENV` | Режим работы | development |
| `JWT_SECRET` | Секретный ключ JWT | (обязательно изменить) |
| `DB_TYPE` | Тип БД | sqlite |
| `CORS_ORIGIN` | Разрешённый источник CORS | http://localhost:3000 |

## 📡 API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Профиль пользователя

### Пользователи
- `GET /api/users` - Список пользователей
- `GET /api/users/:id` - Профиль пользователя
- `PUT /api/users/:id` - Обновление профиля

### Файлы
- `POST /api/upload` - Загрузка файла
- `GET /uploads/:filename` - Скачивание файла

## 🔌 WebSocket

Подключение к WebSocket:

```javascript
const ws = new WebSocket('ws://localhost:3001/ws');

ws.onopen = () => {
    console.log('Connected to WebSocket');
};

ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    console.log('Message:', data);
};
```

## 🚢 Деплой

### Фронтенд (Vercel)

Статические файлы из `public/` деплоятся на Vercel:

1. Подключите репозиторий к Vercel
2. Настройте:
   - **Output Directory**: `public`
   - **Build Command**: (оставить пустым)
3. Добавьте переменные окружения в Vercel

### Бэкенд (Railway/Render/Heroku)

Сервер деплоится на платформу с поддержкой WebSocket:

**Railway:**
1. Подключите репозиторий
2. Укажите `npm start` как start command
3. Добавьте переменные окружения

## 📝 Лицензия

MIT

## 👤 Автор

TechTariff
